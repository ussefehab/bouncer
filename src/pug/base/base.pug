doctype html 
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        title= "BOUNCER"
        link(href="../../../dist/css/styles.min.css" rel="stylesheet")
        block script
            script( src="../../../src/js/logic/megaMenu.js" defer)
            script( src="../../../src/js/logic/routs.js" defer)
            script( src="../../../src/js/logic/mobileMenu.js" defer)
    body
        block header    
        main     
            block carousel
            block banners
            block best
            block banner 
            block offers 
            block latest 
            block feature
            block search
            block pop up
            block breadcrumbs    
            block product-grid 
            block product
            block cart
        block footer